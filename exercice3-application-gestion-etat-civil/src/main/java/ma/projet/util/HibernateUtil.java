package ma.projet.util;

// SessionFactory bean removed: services use JPA EntityManager directly via @PersistenceContext.
// Keeping this class without Spring @Configuration to avoid circular dependencies at startup.
public class HibernateUtil {
}
